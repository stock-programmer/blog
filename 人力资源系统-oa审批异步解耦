这类系统最稳的设计思路是：**审批模块只负责“流程是否通过”，不要直接负责“业务怎么生效”**。审批通过后，通过“领域事件 / 业务事件”通知其他模块，由各模块自己决定如何处理。

可以按这个结构设计：

```mermaid
flowchart LR
  A["员工提交请假/加班/补卡/外出"] --> B["业务单据"]
  B --> C["审批流程实例"]
  C --> D{"审批结果"}
  D -->|通过| E["发布业务事件"]
  D -->|拒绝| F["单据结束"]
  E --> G["考勤模块处理"]
  E --> H["薪资模块处理"]
  E --> I["其他模块处理"]
```

核心拆分成三层：

1. **审批中心**
   只管流程：谁提交、谁审批、当前节点、通过、驳回、撤回、审批日志。

2. **业务单据**
   请假单、加班单、补卡单、外出单本身是业务数据，不应该只是审批表里的 JSON。  
   比如：

   - `leave_request` 请假单
   - `overtime_request` 加班单
   - `punch_correction_request` 补卡单
   - `outing_request` 外出单

3. **业务影响处理**
   审批通过后发布事件，比如：

   - `LeaveApproved`
   - `OvertimeApproved`
   - `PunchCorrectionApproved`
   - `OutingApproved`

   然后考勤模块、薪资模块订阅这些事件。

一个请假通过后的例子：

```text
请假单审批通过
-> 发布 LeaveApproved
-> 考勤模块生成一条“请假影响记录”
-> 薪资模块在算薪时读取请假记录，决定是否扣薪
```

注意，最好不要直接改原始考勤数据。比如员工 9 点打卡、18 点下班，这些原始记录不要改。请假、补卡、外出、加班应该作为“业务事实”叠加进去：

```text
原始打卡记录
+ 请假记录
+ 外出记录
+ 补卡记录
+ 加班记录
= 最终考勤结果
```

这样以后追溯、撤销、重算都清楚。

数据上可以有一张“影响记录表”或“考勤事实表”：

```text
attendance_effect
- id
- employee_id
- effect_type: LEAVE / OVERTIME / PUNCH_CORRECTION / OUTING
- source_type: APPROVAL
- source_id: 请假单ID/加班单ID
- start_time
- end_time
- duration
- status: EFFECTIVE / CANCELLED
- created_at
```

薪资模块也类似，不一定要审批通过后立刻算钱，而是生成或读取业务事实：

```text
payroll_factor
- employee_id
- factor_type: PAID_LEAVE / UNPAID_LEAVE / OVERTIME_PAY
- source_id
- amount 或 duration
- payroll_month
```

关键设计原则：

1. **审批模块不要依赖考勤、薪资、招聘等模块**
   否则以后审批类型越来越多，审批模块会变成大泥球。

2. **用事件解耦**
   审批通过只发布事件，其他模块自己监听。

3. **事件要有业务语义**
   不要只发一个泛泛的 `ApprovalPassed`，最好发 `LeaveApproved`、`OvertimeApproved` 这种明确事件。

4. **处理要幂等**
   同一个审批通过事件可能重复消费，考勤/薪资模块要用 `approval_id + effect_type` 做唯一约束，避免重复生成影响记录。

5. **支持撤销和变更**
   审批通过后如果撤销，请发布对应事件：

   - `LeaveCancelled`
   - `OvertimeCancelled`
   - `PunchCorrectionCancelled`

   下游模块把原影响记录标记为失效，而不是物理删除。

6. **算薪建议按周期重算**
   薪资不要过早固化。月末算薪时读取当月有效的请假、加班、缺勤、补卡等事实，再根据薪资规则计算。

整体可以理解成：

```text
审批 = 决策流程
业务单据 = 申请内容
领域事件 = 通过后的通知
下游模块 = 具体生效逻辑
```

最推荐的架构是：

```text
业务单据创建
-> 发起审批流程
-> 审批完成
-> 单据状态变为已通过
-> 写 Outbox 事件
-> 事件总线投递
-> 考勤/薪资/其他模块消费
-> 生成各自模块的影响记录
```

这套设计扩展性会比较好。以后你增加“调岗审批影响组织架构”、“招聘审批影响 HC 编制”、“离职审批影响账号权限”，都不用改审批核心，只需要增加新的业务事件和消费者。




你这个理解是对的：**审批模板本质上是“表单定义 + 流程定义”的组合**。

为什么要用审批模板？核心原因是：**审批是高频变化的管理规则，不适合写死在代码里**。

比如请假审批：

```text
普通员工请假 1 天：直属上级审批
普通员工请假 3 天：直属上级 -> 部门负责人审批
主管请假：部门负责人 -> HR 审批
销售部门请假：还要抄送区域经理
```

这些规则会随着公司制度、部门、岗位、审批权限变化而变化。如果每次都改代码，系统会很难维护。所以要抽象成审批模板。

一个审批模板可以拆成两部分：

```text
审批模板
= 表单模板
+ 流程模板
```

表单模板负责“员工要填什么”：

```text
请假类型
开始时间
结束时间
请假时长
请假原因
附件
```

这些可以通过控件组合生成：

```text
文本框
日期时间选择器
下拉框
数字输入框
附件上传
人员选择器
部门选择器
明细表格
```

流程模板负责“提交后谁来审”：

```text
发起人
-> 直属上级
-> 部门负责人
-> HR
-> 结束
```

这里就会依赖组织架构，比如：

```text
发起人的直属上级
发起人的部门负责人
某个角色的人
某个岗位的人
某个固定审批人
根据金额/天数/部门走不同分支
```

所以审批模板的价值是：

```text
通过配置，生成可提交的审批表单
通过配置，决定审批如何流转
通过模板版本，保证历史审批可追溯
```

但这里有一个很重要的设计点：**提交审批时，一定要生成审批实例，并保存当时模板的快照或模板版本号**。

因为模板以后可能会改：

```text
4 月提交的请假单，用的是 v1 流程
5 月公司改制度，用的是 v2 流程
```

历史审批不能因为模板被修改而变化。

推荐生命周期是：

```text
管理员配置审批模板
-> 发布模板版本
-> 员工选择审批类型
-> 前端根据表单模板渲染填写页面
-> 员工提交
-> 后端校验表单数据
-> 创建业务单据
-> 创建审批实例
-> 审批实例按流程模板流转
-> 审批通过后发布业务事件
-> 考勤/薪资等模块处理影响
```

可以这样理解：

```text
审批模板：规则说明书
审批实例：某一次真实审批流程
业务单据：这次审批对应的业务内容
```

比如加班：

```text
加班审批模板：
- 表单：加班日期、开始时间、结束时间、加班原因
- 流程：直属上级 -> 部门负责人

加班审批实例：
- 张三在 2026-04-28 提交了一次加班申请
- 当前流转到李四审批
- 审批状态是进行中/已通过/已拒绝
```

更稳的模型是把模板内部拆清楚：

```text
approval_template
- id
- name: 加班审批
- business_type: OVERTIME
- status: draft / published / disabled

approval_template_version
- id
- template_id
- version
- form_schema
- process_schema
- published_at

approval_instance
- id
- template_id
- template_version_id
- business_type
- business_id
- applicant_id
- status
- form_data_snapshot
```

其中：

- `form_schema`：控件定义
- `process_schema`：流程节点定义
- `form_data_snapshot`：本次提交的实际值
- `business_id`：关联加班单、请假单、补卡单等业务单据

这样设计之后，审批系统就既灵活，又不会失控。

一句话总结：

**审批模板是为了把“要填什么”和“谁来审批”配置化；审批实例是模板运行后产生的一次真实审批；审批通过后再通过业务事件影响考勤、薪资等模块。**
